<resources>
    <!-- Base application theme. -->
    <style name="Theme.GreatWeek" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/grey</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/metallic</item>
        <item name="colorSecondaryVariant">@color/metallic_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/backgroundColor</item>
        <!-- Customize your theme here. -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <!-- Bottom sheet dialog -->
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <!--    -->
    <style name="card" parent="@style/Widget.MaterialComponents.CardView">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.MaterialCardView.Cut
        </item>
    </style>

    <style name="ShapeAppearanceOverlay.MaterialCardView.Cut" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">@dimen/card_radius</item>
        <item name="cornerSizeTopLeft">@dimen/card_radius</item>
        <item name="cornerSizeBottomRight">@dimen/card_radius</item>
        <item name="cornerSizeBottomLeft">@dimen/card_radius</item>
    </style>

    <!--    -->
    <style name="title">
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="add_goal_button">
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:text">Add goal</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:drawableLeft">@drawable/ic_add</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!-- Bottom sheet dialog -->
    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_dialog_rounded</item>
    </style>
</resources>